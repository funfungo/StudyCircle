---
description: 新功能完成后提醒添加自动化测试并给出测试方案
alwaysApply: true
---

# 新功能测试提醒

完成新功能开发后，评估是否需要自动化测试。如果判断需要，在告知用户功能完成时同时：

1. **明确提醒**用户该功能建议添加测试
2. **给出测试方案**，包括：
   - 测试文件路径（与被测文件同目录，命名为 `*.test.jsx` / `*.test.ts`）
   - 需要覆盖的关键场景（正常流程、边界情况、错误处理）
   - 简要的测试用例列表

## 需要测试的场景

- 包含业务逻辑或数据处理的组件/函数
- 表单验证、状态管理、条件渲染
- API 调用、数据转换
- 用户交互流程（点击、输入、提交）

## 不需要测试的场景

- 纯展示型组件（无逻辑，只接收 props 渲染）
- 简单的样式调整或文案修改
- 配置文件变更

## 测试方案格式

```markdown
### 建议添加测试

**文件**: `src/components/xxx/Xxx.test.jsx`

**测试用例**:
1. 渲染默认状态
2. 用户交互后状态变化
3. 边界情况处理
4. ...

需要我帮你编写这些测试吗？
```

## 技术栈

- 测试框架：Vitest
- 运行命令：`pnpm test --run`
